// Este archivo es dead code — las funciones que contenía nunca fueron importadas.
// La autenticación se delega completamente a Supabase Auth (AuthContext.jsx).
// Mantenido vacío para no romper posibles imports en entornos de build cacheado.
